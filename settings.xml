<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">

    <localRepository>${user.home}/.m2/repository</localRepository>
    <!--localRepository>${java.io.tmpdir}/.m2/repository</localRepository-->
    <!--localRepository>//NETWORK/DRIVE</localRepository -->

    <interactiveMode>true</interactiveMode>
    <offline>false</offline>

    <!-- <proxies> -->
    <!--     <proxy> -->
    <!--         <active>false</active> -->
    <!--         <id>local-proxy</id> -->
    <!--         <protocol>http</protocol> -->
    <!--         <host>localhost</host> -->
    <!--         <port>7998</port> -->
    <!--         <username>tommy</username> -->
    <!--         <password>...</password> -->
    <!--         <nonProxyHosts>localhost|host1|*.domain</nonProxyHosts> -->
    <!--     </proxy> -->
    <!-- </proxies> -->

    <servers>
        <server>
            <id>central-mirror</id>
            <configuration>
                <connectionTimeout>15000</connectionTimeout> <!-- milliseconds -->
                <readTimeout>15000</readTimeout> <!-- milliseconds -->
            </configuration>
        </server>

        <!-- Set Env.-Variables in Build-Tools/Maven/Runners -->
        <!-- <server> -->
        <!--     <id>secured-mirror</id> -->
        <!--     <username>${env.USER}</username> -->
        <!--     <password>${env.TOKEN}</password> -->
        <!--     <configuration> -->
        <!--         <connectionTimeout>15000</connectionTimeout> -->
        <!--         <readTimeout>15000</readTimeout> -->
        <!--     </configuration> -->
        <!-- </server> -->
    </servers>

    <mirrors>
        <mirror>
            <id>central-mirror</id>
            <mirrorOf>*</mirrorOf>
            <url>https://repo.maven.apache.org/maven2</url>
        </mirror>

        <!-- <mirror> -->
        <!--     <id>secured-mirror</id> -->
        <!--     <mirrorOf>*</mirrorOf> -->
        <!--     <url>https://HOST</url> -->
        <!-- </mirror> -->
    </mirrors>

    <profiles>
        <profile>
            <id>default</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <pluginRepositories>
                <pluginRepository>
                    <id>central-mirror</id>
                    <url>https://repo.maven.apache.org/maven2</url>
                    <releases>
                        <enabled>true</enabled>
                        <updatePolicy>daily</updatePolicy>
                        <checksumPolicy>warn</checksumPolicy>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                </pluginRepository>
            </pluginRepositories>
            <repositories>
                <repository>
                    <id>central-mirror</id>
                    <url>https://repo.maven.apache.org/maven2</url>
                    <releases>
                        <enabled>true</enabled>
                        <updatePolicy>daily</updatePolicy>
                        <checksumPolicy>fail</checksumPolicy>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                        <updatePolicy>daily</updatePolicy>
                        <checksumPolicy>fail</checksumPolicy>
                    </snapshots>
                </repository>
            </repositories>
        </profile>
        <profile>
            <id>rate-limit</id>
            <properties>
                <!-- Parallele Downloads reduzieren (Default: 5) -->
                <maven.artifact.threads>2</maven.artifact.threads>

                <!-- Mehr Retries bei 429/503 (Default: 3) -->
                <aether.connector.http.retryHandler.count>5</aether.connector.http.retryHandler.count>

                <!-- Längeres initiales Backoff (Default: 5000ms) -->
                <aether.connector.http.retryHandler.interval>10000</aether.connector.http.retryHandler.interval>

                <!-- Max. Backoff-Obergrenze (Default: 300000ms = 5min) -->
                <aether.connector.http.retryHandler.intervalMax>120000</aether.connector.http.retryHandler.intervalMax>

                <!-- Connections pro Route reduzieren (Default: 50) -->
                <aether.connector.http.maxConnectionsPerRoute>8</aether.connector.http.maxConnectionsPerRoute>
            </properties>
        </profile>
    </profiles>

    <activeProfiles>
        <!-- Order define the Query-Order -->
        <activeProfile>default</activeProfile>
        <activeProfile>rate-limit</activeProfile>
    </activeProfiles>
</settings>
